#include"vertexLayoutElement.hpp"

namespace oxu::OpenGL
{
    VertexLayoutElement::VertexLayoutElement(const unsigned int &type, const unsigned int &count):
    type(type), count(count)
    {

    }
}