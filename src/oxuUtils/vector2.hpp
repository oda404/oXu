
namespace oxu
{
    struct Vector2f
    {
        float x, y;
        Vector2f();
        Vector2f(const float &x, const float &y);
    };

    struct Vector2i
    {
        int x, y;
        Vector2i();
        Vector2i(const int &x, const int &y);
    };
}