
#include<oxu/audio/openal/core.hpp>

namespace oxu::OpenAL
{
    /*ALCdevice *cp_device;
    ALCcontext *cp_context;

    void init()
    {
        cp_device = alcOpenDevice(nullptr);

        if(cp_device)
        {
            cp_context = alcCreateContext(cp_device, nullptr);
            alcMakeContextCurrent(cp_context);
        }
    }

    bool alHasError(int &err)
    {
        if( (err = alGetError()) != AL_NO_ERROR)
        {
            return true;
        }

        return false;

        
    }*/
}